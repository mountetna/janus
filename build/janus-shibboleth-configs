#!/usr/bin/env sh

set -e

if [ -n "$VERBOSE" ]; then
  set -x
fi

for include in /app/build/shibboleth*; do
  cp "$include" /usr/opt/httpd.conf.d/
done

# TODO
# Possibly copy over the shibboleth2.xml.erb from /secrets/ mount

